"use client"

/**
 * @deprecated WhiteboardCanvas
 *
 * This component is DEPRECATED as part of the Option A Master Fix.
 * All rendering logic has been consolidated into `WhiteboardRenderer.tsx`.
 *
 * Do not use this component. It acts as a passive placeholder to prevent build errors
 * until all references are removed.
 */
export default function WhiteboardCanvas({ payload, autoPlay = true }: any) {
  return null
}
